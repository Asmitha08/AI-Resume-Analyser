{
  "name": "ai-resume-analyser",
  "private": true,
  "workspaces": [
    "client",
    "api"
  ],
  "scripts": {
    "build": "npm run build --workspace=client"
  },
  "dependencies": {
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "typescript": "^5.7.3"
  }
}
