{
  "name": "ai-resume-analyser",
  "private": true,
  "workspaces": [
    "client",
    "api"
  ],
  "scripts": {
    "build": "npm run build --workspace=client",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^7.8.0"
  },
  "devDependencies": {
    "prisma": "^7.8.0"
  }
}
