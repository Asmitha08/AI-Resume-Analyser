{
  "name": "ai-resume-analyser",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "npm run build --workspace=client && npm run generate --workspace=server"
  }
}
