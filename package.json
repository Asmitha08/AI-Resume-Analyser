{
  "name": "ai-resume-analyser",
  "private": true,
  "workspaces": [
    "client",
    "api"
  ],
  "scripts": {
    "build": "npm run build --workspace=client && npm run generate --workspace=api"
  }
}
